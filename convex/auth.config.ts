// https://awake-dog-9.clerk.accounts.dev

const authConfig = {
  providers: [
    {
      domain: "https://awake-dog-9.clerk.accounts.dev",
      applicationID: "convex",
    },
  ]
};

export default authConfig;